---
name: wordpress-ja-translation-guide
description: WordPressコア・プラグイン・テーマの文字列(.po/.pot/readme.txt等)を日本語に翻訳する、既存の日本語訳をレビューする、またはtranslate.wordpress.orgへ提案/インポートする前に表記をチェックする際に必ず使うSkill。ja.wordpress.org公式の翻訳スタイルガイド(全角半角ルール・括弧やカギ括弧の使い方・カタカナ語の長音記号・訳語統一ルール・プレースホルダーの扱い・テーマ名やプラグイン名を翻訳しないルールなど)を反映し、表記の一貫性を担保する。さらにPTE(Project Translation Editor)権限の有無によって異なる作業フロー(一括Import可能か、Suggestで一件ずつ提案するしかないか)も案内する。「WordPressの文字列を日本語に訳して」「.poファイルを翻訳して」「この訳、規約通りになってる?」「PTEとしてインポートする前にチェックして」「このプラグインに翻訳を提案したい」のような依頼が来たら、このSkillを必ず参照すること。
---

# WordPress 日本語翻訳スタイルガイド Skill

[ja.wordpress.org 公式の翻訳ハンドブック](https://ja.wordpress.org/team/handbook/translation/)・[翻訳スタイルガイド](https://ja.wordpress.org/team/handbook/translation/translation-style-guide/)に基づくSkill。

## このSkillを使う場面

- WordPressプラグイン/テーマ/コアの`.po`・`.pot`文字列を日本語に翻訳するとき
- 既存の日本語訳をレビュー・修正するとき
- `translate.wordpress.org`へ提案する、またはPTE権限でインポートする前に、規約違反がないか最終チェックするとき
- readme.txtやドキュメントの日本語訳を作るとき

## 作業を始める前に: PTE権限の確認

翻訳の作業フローは、ユーザーが**対象プロジェクトについて**PTE(Project Translation Editor)権限を持っているかどうかで大きく異なる(一括Importできるか、Suggestで一件ずつ提案するしかないか)。会話内でまだ明らかでなければ、作業に取り掛かる前にユーザーに確認すること。PTE権限はプロジェクトごとに付与されるため、「ある特定のプラグインではPTEだが、別のプラグインでは権限なし」という状態もふつうにあり得る。

詳しい確認方法・2つのフローの違いは `references/contribution-workflow.md` を参照すること。

## 最重要: 機械翻訳の精査義務

ja.wordpress.org公式ハンドブックは次のように明記している:「**機械翻訳の結果を精査せずに使った翻訳および翻訳の提案はしないこと**。必ず翻訳スタイルガイドに合わせた修正を行うこと。精査が行われていないと承認者が判断した場合、提案を一括拒否される場合がある」。

このSkillによる訳文生成もこの「機械翻訳」に該当する。したがって:

- 生成した訳文は**必ず人間がレビューしてから提案・インポートする**前提で扱う
- Skill自身が「これで完成」と断定する出力をしない
- 自信のない訳語は確定させず `[要確認: ◯◯]` の形で明示し、判断を人間に委ねる

## 翻訳の基本方針

WordPressプロジェクトが掲げる3つの執筆・翻訳指針:

1. **分かりやすさ**: 技術者以外の人にも分かりやすく。フレンドリーで明確、簡潔な能動態の文章にする
2. **独自性**: 英語原文のトーンを尊重し、直訳ではなく意味や感情を伝える。原文のジョークや引用が日本語文化で通用しない場合は、削除するか言い回しを変えて意味を通す
3. **現代的であること**: 最新の技術用語・言語の進化に追従し、時代遅れに見えない表現を使う

## 必ず守る表記ルール(特に間違いやすい点)

1. 英字・数字・英記号は半角。`!` や `?` も半角にする
2. 数字の前後には半角スペースを入れない(例: `横640ピクセル`、`2018年8月4日`、`%d件のエラー`)
3. 文頭以外の英字・記号の前後には半角スペースを入れる(例: `警告 !`、`WordPress ドキュメンテーション`)。ただし『』「」。、の前後とコロン(:)の前にはスペースを入れない
4. 丸括弧`()`は半角にし、外側に半角スペースを入れる。内側には入れない。直後に句読点が続く場合はスペースを省く(例: `言語コードは ja (日本語)。`)
5. 原文が大文字で固有名詞を区別している部分(メニュー項目・ボタンラベルなど)は、日本語ではカギ括弧「」で囲む
6. カタカナ語の長音記号「ー」は、長音を含めて4文字以下の単語には付け、5文字以上には付けない(例外あり、詳細は references/notation-rules.md)
7. カタカナの複合語に中点「・」は原則使わない(用語集で指定がある場合を除く)
8. PHPのdateフォーマット文字列(`m/d/Y g:i:s A`など)はコードとして保持し、翻訳しない

これらの詳細・例外・全例文は `references/notation-rules.md` を必ず参照すること(このSKILL.md本体には要点のみ記載)。

## 訳語選択・文体の統一ルール

- 受動態はなるべく避ける(「〜されました」ではなく「〜しました」)
- "View XX" は「〜を表示」に統一(「閲覧」「見る」「開く」「参照」は使わない)
- "XX is/are not allowed to…" は「〜する権限がありません」に統一
- "Sorry, …" で始まるエラーメッセージは「すみません」と訳さず、その部分を削除する
- "You"/"Your" は日本語で不自然になる場合は訳出しない(「自分の」「お使いの」などに言い換えるか省略)
- 「下さい」ではなく「ください」、「全て」ではなく「すべて」、「既に」ではなく「すでに」を使う
- 見出しは体言止めまたは「する」(常体)、箇条書きは「する」(常体)、ボタンラベルは「します」を省略した形にする
- メニュー項目・ボタンラベルの訳語は、同一プロジェクト内の他の文字列と必ず統一する

詳細・実例は `references/word-choice-rules.md` を参照すること。

## プレースホルダー(`%s` `%d` `%1$s` など)

- 原文に含まれるプレースホルダーの**数と種類は訳文でも完全に一致**させる。一つでも欠けたり増えたりするとPHPエラーの原因になる
- 順序を入れ替える必要がある場合は、番号付き(`%1$s`、`%2$s`)に変換してから並べ替える(無番号のまま順序を変えてはいけない)
- 訳文を出力したら、必ず「原文のプレースホルダー一覧」と「訳文のプレースホルダー一覧」を見比べて一致を確認する

## 翻訳しないもの

- **WordPress**: 常に「WordPress」と表記し、翻訳・音訳しない
- **テーマ名・プラグイン名**: 例えば"Twenty Twenty"のようなテーマ名は翻訳しない。プラグイン名も同様
- **機能名**: [公式用語集](https://translate.wordpress.org/locale/ja/default/glossary)に従う。新しい用語は独自に確定させず`[要確認]`とする
- ショートコードタグ、HTMLタグ、変数名、関数名

## 用語集・表記ゆれチェックツールの活用

- 訳語選択に迷ったら、まず[公式用語集](https://translate.wordpress.org/locale/ja/default/glossary)を確認する(このSkillは用語集を全件収録していない)
- 既存の承認済み訳との表記ゆれは [Consistency Tool](https://translate.wordpress.org/consistency) で確認できる。同じ原文がプロジェクト間でどう訳されているか調べられる
- 複数の訳語候補があり判断がつかない場合や、既存訳が誤っていると思われる場合は、訳文を確定させず `[要確認: WordSlack #translate で要相談]` と明記する

## 出力フォーマット

`.po`形式で出力する場合は、以下の構造を保つこと:

```
#: path/to/file.php:123
msgid "原文の文字列"
msgstr "訳文の文字列"
```

- `msgid`は変更しない(原文そのまま)
- `msgstr`にのみ訳文を入れる
- 複数形(`msgid_plural`)がある場合は、日本語は単数/複数の区別がないため同じ訳文を使ってよいが、`msgstr[0]`の形式は保持する

## 出力後のセルフチェックリスト

訳文を出力する際は、必ず以下を確認してから提示すること:

- [ ] プレースホルダーの数・種類が原文と一致しているか
- [ ] テーマ名・プラグイン名・「WordPress」表記・機能名を誤って翻訳していないか
- [ ] 全角半角ルール(英数字・記号は半角、数字前後はスペースなし)に沿っているか
- [ ] 括弧・カギ括弧の使い方が規約通りか
- [ ] 受動態を避け、能動態・統一された語尾になっているか
- [ ] 「ください」「すべて」「すでに」など指定の表記を使っているか
- [ ] 既存の承認済み訳と表記が揺れていないか(Consistency Toolで確認推奨)
- [ ] 用語選択に確信が持てない箇所は `[要確認]` と明記したか
- [ ] 対象プロジェクトでのPTE権限の有無を確認し、適切な反映フロー(Import / Suggest)を案内したか

## 重要な注意

- このSkillはja.wordpress.org公式の用語集(glossary)を全件収録していない。判断に迷う訳語は確定させず、`[要確認]`として明示すること
- このSkillが生成した訳文は**ドラフトであり最終版ではない**。`translate.wordpress.org`への反映(提案・インポートいずれも)は、必ず人間(まーちゅう)が目視レビューした後に行うこと。Skillが「これで完成」と断定する出力をしてはいけない
- PTE非保有プロジェクトでは、Suggest操作の自動化(無人での一括投稿など)は行わない。提案は必ずユーザー本人が一件ずつtranslate.wordpress.org上で行う(詳細は references/contribution-workflow.md)
